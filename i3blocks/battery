#!/bin/bash
BAT=$(ls /sys/class/power_supply/BAT* 2>/dev/null | head -1)
if [ -n "$BAT" ]; then
    CAPACITY=$(cat "$BAT/capacity" 2>/dev/null)
    REMAINING=$(cat "$BAT/time_to_empty" 2>/dev/null)
    echo "BAT $CAPACITY%"
else
    echo "BAT N/A"
fi
